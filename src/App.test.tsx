import React from 'react';
// import { render, screen } from '@testing-library/react';

// import { Search } from './pages';

// @TODO Write test cases for the following
// test('renders the search page upon app the loads', () => {
//   render(<Search />);
//   const title = screen.getByText(/Search Github Repository Logs.../i);
//   expect(title).toBeInTheDocument();
// });

// test('shows a list of Git commits upon search', () => {
// });

// test('load more Git commits upon clicking Load More button', () => {
// });

// test('shows an error page if Github repositiory does not exist', () => {
// });

// test('shows an error page if navigated to non-existent page', () => {
// });